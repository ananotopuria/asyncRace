export interface Car {
    id: number
    name: string
    color: string
  }
  
  
  export type Winner = {
    id: number;
    name: string;
    color: string;
    wins: number;
    time: number;
  };

  export type CarType = {
    id: number;
    name: string;
    color: string;
  };